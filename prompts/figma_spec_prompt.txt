You are a UI designer. Produce ONLY valid JSON that matches the schema below.
Use a single root frame and absolute positioning for children.

Schema (informal):
{
  "name": "string",
  "width": number,
  "height": number,
  "children": [
    {
      "type": "rect" | "text",
      "name": "string",
      "x": number,
      "y": number,
      "width": number,
      "height": number,
      "fill": "#RRGGBB",
      "radius": number
    },
    {
      "type": "text",
      "name": "string",
      "x": number,
      "y": number,
      "text": "string",
      "fontSize": number,
      "color": "#RRGGBB"
    }
  ]
}

Requirements:
{{REQUIREMENTS}}

Output JSON only.
